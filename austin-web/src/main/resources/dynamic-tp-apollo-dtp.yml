# 动态线程池配置文件，建议单独开一个文件放到配置中心，字段详解看readme介绍
spring:
  dynamic:
    tp:
      enabled: true
      enabledBanner: true           # 是否开启banner打印，默认true
      enabledCollect: true          # 是否开启监控指标采集，默认false
      collectorType: micrometer     # 监控数据采集器类型（JsonLog | MicroMeter），默认logging
      monitorInterval: 5            # 监控时间间隔（报警判断、指标采集），默认5s
      apollo:                       # apollo配置，不配置默认拿apollo配置第一个namespace
        namespace: dynamic-tp-apollo-dtp.yml
      configType: yml
      platforms:
        - platform: wechat
          urlKey: 38aa7eff500-1287
          receivers: apollo
        - platform: ding
          urlKey: f80dad441fcd65bac48473d4a88dcd6a
          secret: SECb544445a6a34f0315d08b17de41
          receivers: 18888888888
      executors:
        - threadPoolName: austin.im.notice
          corePoolSize: 6
          maximumPoolSize: 8
          queueCapacity: 200
          queueType: VariableLinkedBlockingQueue   # 任务队列，查看源码QueueTypeEnum枚举类
          rejectedHandlerType: CallerRunsPolicy    # 拒绝策略，查看RejectedTypeEnum枚举类
          keepAliveTime: 50
          allowCoreThreadTimeOut: false
          threadNamePrefix: austin-           # 线程名前缀
          notifyItems: # 报警项，不配置自动会配置（变更通知、容量报警、活性报警、拒绝报警）
            - type: capacity               # 容量报警：队列使用率超过阈值时报警
              enabled: true
              threshold: 80                # 报警阈值
              platforms: [ding,wechat]     # 可选配置，不配置默认拿上层platforms配置的所以平台
              interval: 120                # 报警间隔（单位：s）
            - type: change                 # 变更通知：线程池配置变更时通知
              enabled: true
              platforms: [ding,wechat]
            - type: liveness               # 活性报警：线程池活跃度超过阈值时报警
              enabled: true
              threshold: 80
              platforms: [ding,wechat]
              interval: 120
            - type: reject                 # 拒绝报警：任务被拒绝时报警
              enabled: true
              threshold: 1
              platforms: [ding,wechat]
              interval: 120
            - type: run_timeout            # 运行超时报警
              enabled: true
              threshold: 5000              # 运行超时时间（单位：ms）
              platforms: [ding,wechat]
              interval: 120
            - type: queue_timeout          # 队列等待超时报警
              enabled: true
              threshold: 2000              # 队列等待超时时间（单位：ms）
              platforms: [ding,wechat]
              interval: 120
        - threadPoolName: execute-xxl-thread-pool
          corePoolSize: 3
          maximumPoolSize: 3
          queueCapacity: 200
          queueType: VariableLinkedBlockingQueue   # 任务队列，查看源码QueueTypeEnum枚举类
          rejectedHandlerType: CallerRunsPolicy    # 拒绝策略，查看RejectedTypeEnum枚举类
          keepAliveTime: 50
          allowCoreThreadTimeOut: false
          threadNamePrefix: austin-           # 线程名前缀
          notifyItems: # 报警项，不配置自动会配置（变更通知、容量报警、活性报警、拒绝报警）
            - type: capacity               # 容量报警：队列使用率超过阈值时报警
              enabled: true
              threshold: 80                # 报警阈值
              platforms: [ding,wechat]     # 可选配置，不配置默认拿上层platforms配置的所以平台
              interval: 120                # 报警间隔（单位：s）
            - type: change                 # 变更通知：线程池配置变更时通知
              enabled: true
              platforms: [ding,wechat]
            - type: liveness               # 活性报警：线程池活跃度超过阈值时报警
              enabled: true
              threshold: 80
              platforms: [ding,wechat]
              interval: 120
            - type: reject                 # 拒绝报警：任务被拒绝时报警
              enabled: true
              threshold: 1
              platforms: [ding,wechat]
              interval: 120